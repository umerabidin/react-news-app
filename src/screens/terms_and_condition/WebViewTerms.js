import {WebView} from 'react-native-webview'
import {
    StyleSheet, View, Text, Image, TouchableOpacity, SafeAreaView,
    Keyboard,
    ScrollView,
    Alert,
  } from 'react-native';

const TermsAndCondition = () => {
return (
 <WebView source={{uri:'https://bitverx.io/'} }/>
     
        
);
}

export default TermsAndCondition;